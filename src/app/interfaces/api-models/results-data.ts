export interface ResultsData {
  teamId: string;
  apertura?: (number | null)[];
  clausura?: (number | null)[];
  regional?: (number | null)[];
  grupos?: (number | null)[];
  final?: (number | null)[];
}